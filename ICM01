
#include <stdio.h>
int main()
{
    long long int t,k,n,i,sum,sum1;
    scanf("%lld",&t);
    while(t--)
    { sum=0;
        scanf("%lld%lld",&n,&k);
            sum=k*(k+1)/2;
            sum1=n-k;
        printf("%lld\n",sum+sum1);
    }
    return 0;
}
